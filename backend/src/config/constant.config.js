const UserRoles = {
    ADMIN: "admin",
    USER: "user"
}

const Status = {
    ACTIVE :"active",
    INACTIVE: "inactive",
    PENDING: "pending"
}

const TaskStatus = {
    ACTIVE: 'active',
    COMPLETED: "completed",
    
}

module.exports = {UserRoles, Status, TaskStatus}